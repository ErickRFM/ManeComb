name: Close durable chat delivery

on:
  push:
    branches:
      - agent/communication-closure

permissions:
  contents: write

jobs:
  close-phase:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/communication-closure
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Apply durable chat patch
        run: python scripts/close-chat-idempotency.py
      - name: Install backend
        working-directory: backend
        run: npm ci
      - name: Test backend
        working-directory: backend
        run: npm test
      - name: Install mobile
        working-directory: mobile
        run: npm ci
      - name: Typecheck mobile
        working-directory: mobile
        run: npm run typecheck
      - name: Test mobile
        working-directory: mobile
        run: npm test -- --runInBand
      - name: Commit closed phase
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/communication-chat-idempotency.yml scripts/close-chat-idempotency.py
          git add mobile/src backend/src backend/test
          git commit -m "fix(chat): make message delivery idempotent"
          git push origin HEAD:agent/communication-closure
